<h1>Add a Bookmark</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for bookmarks</p>
    <ul>
      <li>Make sure the link works</li>
      <li>Copy and paste the link for best results.</li>
      <li>Try not to save duplicate websites.</li>
    </ul>
  </div>

  <div class="col-md-8">
    <%= form_for [@topic, @bookmark] do |f| %>
      <div class="form-group">
      <%= f.label :url %>
      <%= f.text_field :url, class: 'form-control', placeholder: "Enter website address (URL)" %>
  </div>
  <div class="form-group">
    <input type="submit" name="commit" value="Save" class="btn btn-success"> -->
    <%= f.submit "Save", class: 'btn btn-success' %>
  </div>
  <% end %>
</div>

